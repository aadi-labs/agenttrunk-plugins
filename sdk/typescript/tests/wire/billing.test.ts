// This file was auto-generated by Fern from our API Definition.

import * as AgentTrunkApi from "../../src/api/index";
import { AgentTrunkClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("BillingClient", () => {
    test("get (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = {
            plan: "free",
            accesses: 1,
            includedAccesses: 1,
            spendLimitCents: 1,
            meteringActive: "meteringActive",
            canManage: true,
            subscriptionStatus: "subscriptionStatus",
            monthlyPriceCents: 1,
            overageCents: 1,
            overageCentsPerThousand: 1,
            period: "period",
            hasCustomer: true,
        };

        server.mockEndpoint().get("/v1/billing").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.billing.get();
        expect(response).toEqual(rawResponseBody);
    });

    test("get (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/v1/billing").respondWith().statusCode(403).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.billing.get();
        }).rejects.toThrow(AgentTrunkApi.ForbiddenError);
    });

    test("get (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/v1/billing").respondWith().statusCode(503).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.billing.get();
        }).rejects.toThrow(AgentTrunkApi.ServiceUnavailableError);
    });

    test("createCheckout (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { plan: "starter" };
        const rawResponseBody = { url: "url" };

        server
            .mockEndpoint()
            .post("/v1/billing/checkout")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.billing.createCheckout({
            plan: "starter",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("createCheckout (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { plan: "starter" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/checkout")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.createCheckout({
                plan: "starter",
            });
        }).rejects.toThrow(AgentTrunkApi.BadRequestError);
    });

    test("createCheckout (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { plan: "starter" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/checkout")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.createCheckout({
                plan: "starter",
            });
        }).rejects.toThrow(AgentTrunkApi.ForbiddenError);
    });

    test("createCheckout (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { plan: "starter" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/checkout")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.createCheckout({
                plan: "starter",
            });
        }).rejects.toThrow(AgentTrunkApi.ConflictError);
    });

    test("createCheckout (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { plan: "starter" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/checkout")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(503)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.createCheckout({
                plan: "starter",
            });
        }).rejects.toThrow(AgentTrunkApi.ServiceUnavailableError);
    });

    test("createPortal (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { url: "url" };

        server
            .mockEndpoint()
            .post("/v1/billing/portal")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.billing.createPortal();
        expect(response).toEqual(rawResponseBody);
    });

    test("createPortal (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/portal")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.createPortal();
        }).rejects.toThrow(AgentTrunkApi.ForbiddenError);
    });

    test("createPortal (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/portal")
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.createPortal();
        }).rejects.toThrow(AgentTrunkApi.ConflictError);
    });

    test("createPortal (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/portal")
            .respondWith()
            .statusCode(503)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.createPortal();
        }).rejects.toThrow(AgentTrunkApi.ServiceUnavailableError);
    });

    test("setSpendLimit (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { cents: 1 };
        const rawResponseBody = { spendLimitCents: 1 };

        server
            .mockEndpoint()
            .post("/v1/billing/spend-limit")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.billing.setSpendLimit({
            cents: 1,
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("setSpendLimit (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { cents: 1000000 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/spend-limit")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.setSpendLimit({
                cents: 1000000,
            });
        }).rejects.toThrow(AgentTrunkApi.BadRequestError);
    });

    test("setSpendLimit (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { cents: 1000000 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/spend-limit")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.setSpendLimit({
                cents: 1000000,
            });
        }).rejects.toThrow(AgentTrunkApi.ForbiddenError);
    });

    test("setSpendLimit (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { cents: 1000000 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/billing/spend-limit")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(503)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.billing.setSpendLimit({
                cents: 1000000,
            });
        }).rejects.toThrow(AgentTrunkApi.ServiceUnavailableError);
    });
});
