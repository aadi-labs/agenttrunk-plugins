// This file was auto-generated by Fern from our API Definition.

import { AgentTrunkClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ScopesClient", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = {
            data: [
                {
                    canCreateScope: true,
                    id: "id",
                    name: "name",
                    slug: "slug",
                    createdBy: "createdBy",
                    createdAt: "2024-01-15T09:30:00Z",
                    environments: [{ id: "id", name: "staging", commitSha: null }],
                },
            ],
        };

        server
            .mockEndpoint()
            .get("/v1/trunks/trunkId/scopes")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.scopes.list("trunkId");
        expect(response).toEqual(rawResponseBody);
    });

    test("create", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = { name: "name" };
        const rawResponseBody = {
            canCreateScope: true,
            id: "id",
            name: "name",
            slug: "slug",
            createdBy: "createdBy",
            createdAt: "2024-01-15T09:30:00Z",
            environments: [
                {
                    id: "id",
                    name: "staging",
                    commitSha: "commitSha",
                    canDeploy: true,
                    canPropose: true,
                    canPublish: true,
                },
            ],
        };

        server
            .mockEndpoint()
            .post("/v1/trunks/trunkId/scopes")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.scopes.create("trunkId", {
            name: "name",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
