// This file was auto-generated by Fern from our API Definition.

import * as AgentTrunkApi from "../../src/api/index";
import { AgentTrunkClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ContextSetsClient", () => {
    test("sources (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/context-sources")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.contextSets.sources();
        expect(response).toEqual(rawResponseBody);
    });

    test("sources (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/context-sources")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.contextSets.sources();
        }).rejects.toThrow(AgentTrunkApi.BadRequestError);
    });

    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/v1/context-sets").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.contextSets.list();
        expect(response).toEqual(rawResponseBody);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/v1/context-sets").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.contextSets.list();
        }).rejects.toThrow(AgentTrunkApi.BadRequestError);
    });

    test("create", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });
        const rawRequestBody = {
            name: "name",
            sources: [
                {
                    sourceTrunkId: "sourceTrunkId",
                    sourceScopeId: "sourceScopeId",
                    environmentId: "environmentId",
                    contextKey: "contextKey",
                    revisionId: "revisionId",
                    packageDigest: "packageDigest",
                    mountPath: "mountPath",
                    required: true,
                },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/context-sets")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.contextSets.create({
            name: "name",
            sources: [
                {
                    sourceTrunkId: "sourceTrunkId",
                    sourceScopeId: "sourceScopeId",
                    environmentId: "environmentId",
                    contextKey: "contextKey",
                    revisionId: "revisionId",
                    packageDigest: "packageDigest",
                    mountPath: "mountPath",
                    required: true,
                },
            ],
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("resolve (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/context-sets/contextSetId/resolve")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.contextSets.resolve("contextSetId");
        expect(response).toEqual(rawResponseBody);
    });

    test("resolve (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentTrunkClient({ maxRetries: 0, accessToken: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/context-sets/contextSetId/resolve")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.contextSets.resolve("contextSetId");
        }).rejects.toThrow(AgentTrunkApi.ForbiddenError);
    });
});
